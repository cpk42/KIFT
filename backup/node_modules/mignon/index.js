`use strict`

function mignon(cutlet){
	const steak = cutlet();
	function filet(meat){
		if (meat.done) return meat.value;
		const p = meat.value;
		return p.then( serving => filet(steak.next(serving)))
	}
	return filet(steak.next());
}

module.exports = mignon
